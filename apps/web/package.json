{
  "name": "@glitter-atlas/web",
  "private": true,
  "scripts": {
    "dev": "bash -lc 'set -a; source ../../.env 2>/dev/null || true; source .env.development 2>/dev/null || true; source .env.local 2>/dev/null || true; set +a; next dev --hostname 127.0.0.1 --port 4000'",
    "build": "next build",
    "start": "next start --hostname 127.0.0.1 --port 4000",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.888.0",
    "@glitter-atlas/shared": "workspace:*",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2"
  }
}
